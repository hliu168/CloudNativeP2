pipeline {
    agent any
    options { 
        timestamps()
        ansiColor('xterm')
        disableConcurrentBuilds()
        skipDefaultCheckout(true)
    }
    stages {
        stage('Prepare Build Env') {
            steps {
                script {
                    cleanWs()
                    deleteDir()
                    checkout scm
                    sh "printenv"
                }
            }
        }
    }
}
